# テスト方針と結果

## 現在の検証範囲

| 区分 | 状態 | 内容 |
| --- | --- | --- |
| 文書構造 | 実施済み | 必須ファイル、見出し、YAML、内部リンク |
| 必須規則の静的検査 | 実施済み | 非二重登録、安全な移管、状態優先順位などの記載を検査 |
| 公開テストケース定義 | 実施済み | 下記44ケースを定義 |
| ケース別の外部動作検証 | 未実施 | 利用者の検証環境で実施する |
| Trello実操作 | 未実施 | 利用者の検証環境で実施する |
| Google Calendar実操作 | 未実施 | 利用者の検証環境で実施する |
| Trello→Calendar実移管 | 未実施 | 利用者の検証環境で実施する |

「実施済み」は文書とリポジトリの静的検証を意味し、外部サービスの動作保証ではありません。

## 自動検証

```bash
python3 tests/validate_repository.py
```

## 公開テストケース

### 入力・分類

1. 名称なし → 作成なし・エラー
2. 名称のみ → Trello通常タスク
3. 期限のみ → Trello、Calendarなし
4. 実行日＋時刻 → Calendar、Trelloなし
5. 実行日のみ＋明確なイベント → 終日予定候補
6. 「予定日」の意味が曖昧 → 確認し、無断登録なし
7. 開始日のみ → Trello
8. 開始日＋期限 → Trello、ガント生成可能
9. 複数の独立内容 → 内容ごとに振り分け
10. Calendar登録「しない」 → 両方未登録

### 重複・移管

11. Calendar完全重複 → 新規作成停止
12. 類似名＋同日＋時間重複 → 人間確認
13. 同名だが別日 → 登録可能
14. 移管先の既存予定を確認 → 確認後だけ元カードをアーカイブ
15. Calendar新規登録と再確認成功 → 元カードをアーカイブ
16. Calendar作成失敗 → 元カードを維持
17. Calendar再確認失敗 → 元カードを維持
18. Trelloアーカイブ失敗 → 移管未完了として報告

### 障害・権限

19. Calendar利用不能 → Trelloへ代替しない
20. Trello利用不能 → 依存する後続処理を停止
21. 読み取り権限のみ → 書き込まず報告
22. タイムアウト → 成功扱いにせず再確認
23. 部分失敗 → 独立項目は継続し、依存項目は停止

### 状態判定

24. 完了が主状態の最優先
25. 前提未完了 → ブロック中
26. 前提なしまたは前提完了＋未完了＋期限超過 → 主状態は期限超過
27. 将来の開始日 → 開始待ち
28. 開始日なし → 開始日未設定
29. 条件充足 → 実行可能
30. 説明欄だけ「完了」 → 警告のみ
31. 存在しない前提カード → エラー
32. 前提未完了＋期限超過 → 主状態はブロック中、警告は期限超過

### 日時境界

33. 当日期限で時刻なし → 日単位の判定規則を使用
34. タイムゾーン → 利用環境または指定値を使用
35. 日付をまたぐ予定 → 開始・終了日時を保持
36. 終日予定 → 明確なイベントだけを候補にする
37. うるう日・月末・年越し → 実在日時を検証
38. 終了時刻なし、または終了が開始以前 → 確認またはエラー

### 非破壊性・公開安全性

39. 読み取り指示 → 外部変更ゼロ
40. 推定日時 → 正式予定として登録しない
41. 同じ意味の情報 → 二重登録しない
42. Calendar失敗 → Trelloへ代替しない
43. Calendar再確認前 → 元カードをアーカイブしない
44. 公開fixture → 個人情報、実在URL、ID、認証情報を含めない

## 結合テストを行う場合

検証専用のボードとカレンダーを使用し、作成・検索・変更・削除・移管を一件ずつ確認します。実データや認証情報はコミットせず、結果には匿名化した日時、成否、期待結果との差だけを記録してください。
